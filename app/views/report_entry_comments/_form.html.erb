<%= semantic_form_for report_entry_comment do |f| %>
	<%= f.inputs do %>
		<%= f.input :administrator_id, :as => :hidden %>
		<%= f.input :report_entry_id, :as => :hidden %>
		<%= f.input :body, :input_html => { :rows => 3 }%>
		<%= f.actions %>
	<% end %>
	<% f.actions %>
<% end %>
